# ────────────────
# Core del sitio
# ────────────────
# URL base del sitio usada para canonicals y OG (si no se define, se usa http://localhost:3000 en dev)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Idioma por defecto y lista de locales (mantener en sync con src/i18n/locales.ts)
NEXT_PUBLIC_DEFAULT_LOCALE=es
NEXT_PUBLIC_AVAILABLE_LOCALES=es,en

# Nombre público del sitio (si no lo usas desde src/config/site.ts, puedes ignorarlo)
NEXT_PUBLIC_SITE_NAME=Black Crow

# ────────────────
# SEO / Social (opcionales)
# ────────────────
# Imagen OG por defecto (ruta absoluta o relativa al dominio)
NEXT_PUBLIC_OG_IMAGE=/images/og-default.png
NEXT_PUBLIC_TWITTER_HANDLE=@blackcrow

# ────────────────
# Revalidación / ISR (opcional)
# ────────────────
# Secreto para revalidación on-demand (si implementas un endpoint para ello)
REVALIDATE_SECRET=

# ────────────────
# Analytics (opcionales)
# ────────────────
# Proveedor: noop | ga4 | plausible (solo referencial; integra el que uses)
ANALYTICS_PROVIDER=noop
# ID de tracking, p.ej. GA4: G-XXXXXXX
NEXT_PUBLIC_ANALYTICS_ID=

# ────────────────
# API internas (placeholders)
# ────────────────
MAILER_PROVIDER=noop         # noop | resend | smtp
MAIL_FROM=
MAIL_TO_DEFAULT=

NEWSLETTER_PROVIDER=noop     # noop | mailchimp | resend
NEWSLETTER_API_KEY=
NEWSLETTER_LIST_ID=

# ────────────────
# Seguridad / Rate limit / Infra (opcionales)
# ────────────────
CORS_ALLOWED_ORIGINS=        # ej: https://tusitio.com,https://admin.tusitio.com
RATE_LIMIT_WINDOW=60         # en segundos
RATE_LIMIT_MAX=30
REDIS_URL=                   # si usas Redis para cache/ratelimit

# ────────────────
# Observabilidad (cliente y servidor)
# ────────────────
# Habilita envío de Web Vitals desde el cliente en desarrollo (en prod se envía por defecto)
NEXT_PUBLIC_ENABLE_VITALS=0

# OpenTelemetry (Node) — opcional. Requiere @opentelemetry/* instalados.
ENABLE_OTEL=0
OTEL_SERVICE_NAME=black-crow-web
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318/v1/traces

# Sentry (opcional; no integrado por defecto)
SENTRY_DSN=
LOG_LEVEL=info               # debug | info | warn | error
