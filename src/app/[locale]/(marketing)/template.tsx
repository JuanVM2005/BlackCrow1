// src/app/[locale]/(marketing)/template.tsx
import type { ReactNode } from "react";

export default function MarketingTemplate({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
